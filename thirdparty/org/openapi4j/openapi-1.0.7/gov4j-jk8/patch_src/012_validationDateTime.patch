--- openapi-schema-validator/src/main/java/org/openapi4j/schema/validator/v3/FormatValidator.java	2025-01-02 16:29:13.786180707 +0100
+++ openapi-schema-validator/src/main/java/org/openapi4j/schema/validator/v3/FormatValidator.java	2025-01-03 11:23:09.726180702 +0100
@@ -21,7 +21,7 @@
  * <p/>
  * <a href="https://tools.ietf.org/html/draft-wright-json-schema-validation-00#page-13" />
  */
-class FormatValidator extends BaseJsonValidator<OAI3> {
+public class FormatValidator extends BaseJsonValidator<OAI3> {
   private static final ValidationResult ERR = new ValidationResult(ERROR, 1007, "Value '%s' does not match format '%s'.");
   private static final ValidationResult UNKNOWN_WARN = new ValidationResult(WARNING, 1008, "Format '%s' is unknown, validation passes.");
 
@@ -29,7 +29,9 @@
 
   private static final Pattern BASE64_PATTERN = Pattern.compile("^([A-Za-z0-9+/]{4})*([A-Za-z0-9+/]{4}|[A-Za-z0-9+/]{3}=|[A-Za-z0-9+/]{2}==)$");
   private static final Pattern DATE_PATTERN = Pattern.compile("^\\d{4}-(?:0[0-9]{1}|1[0-2]{1})-(0?[1-9]|[12][0-9]|3[01])$");
-  private static final Pattern DATETIME_PATTERN = Pattern.compile("^\\d{4}-(?:0[0-9]{1}|1[0-2]{1})-(0?[1-9]|[12][0-9]|3[01])[tT ]\\d{2}:\\d{2}:\\d{2}(\\.\\d+)?([zZ]|[+-]\\d{2}:\\d{2})$");
+  private static final String DATETIME_PATTERN_SEPARATOR = "tT ";
+  private static final String DATETIME_PATTERN_TIMEZONE = "zZ";
+  private static final String DATETIME_PATTERN = "^\\d{4}-(?:0[0-9]{1}|1[0-2]{1})-(0?[1-9]|[12][0-9]|3[01])["+DATETIME_PATTERN_SEPARATOR+"]\\d{2}:\\d{2}:\\d{2}(\\.\\d+)?(["+DATETIME_PATTERN_TIMEZONE+"]|[+-]\\d{2}:\\d{2})$";
   private static final Pattern EMAIL_PATTERN = Pattern.compile("^\\S+@\\S+$");
   private static final Pattern HOSTNAME_PATTERN = Pattern.compile("^([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])(\\.([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9]))*$");
   private static final Pattern IPV4_PATTERN = Pattern.compile("^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$");
@@ -39,6 +41,60 @@
 
   private final String format;
 
+  // https://datatracker.ietf.org/doc/html/rfc3339#section-5.6 
+  private static boolean dateTimeAllowLowerCaseTZ = false;
+  private static boolean dateTimeAllowSpaceSeparator = false;
+  public static boolean isDateTimeAllowLowerCaseTZ() {
+	return dateTimeAllowLowerCaseTZ;
+  }
+  public static void setDateTimeAllowLowerCaseTZ(boolean v) {
+	dateTimeAllowLowerCaseTZ = v;
+	clearDateTimePattern();
+  }
+  public static boolean isDateTimeAllowSpaceSeparator() {
+	return dateTimeAllowSpaceSeparator;
+  }
+  public static void setDateTimeAllowSpaceSeparator(boolean v) {
+	dateTimeAllowSpaceSeparator = v;
+	clearDateTimePattern();
+  }
+  private static String buildDateTimeSeparatorPattern() {
+	StringBuilder sb = new StringBuilder();
+	sb.append("T");
+	if(dateTimeAllowLowerCaseTZ) {
+		sb.append("t");	
+	}
+	if(dateTimeAllowSpaceSeparator) {
+		sb.append(" ");	
+	}
+	return sb.toString();
+  }
+  private static String buildTimeZonePattern() {
+	StringBuilder sb = new StringBuilder();
+	sb.append("Z");
+	if(dateTimeAllowLowerCaseTZ) {
+		sb.append("z");	
+	}
+	return sb.toString();
+  }
+  private static Pattern dateTimePattern = null;
+  private static void clearDateTimePattern(){
+	dateTimePattern = null;
+  }
+  private static void initDateTimePattern(){
+  	if(dateTimePattern==null){
+	  	String dateTimePatternS = DATETIME_PATTERN.replace(DATETIME_PATTERN_SEPARATOR, buildDateTimeSeparatorPattern());
+		dateTimePatternS = dateTimePatternS.replace(DATETIME_PATTERN_TIMEZONE, buildTimeZonePattern());
+		dateTimePattern = Pattern.compile(dateTimePatternS);
+	}
+  }
+  private static Pattern buildDateTimePattern(){
+  	if(dateTimePattern==null){
+	  	initDateTimePattern();
+  	}
+  	return dateTimePattern;
+  }
+
   FormatValidator(final ValidationContext<OAI3> context, final JsonNode schemaNode, final JsonNode schemaParentNode, final SchemaValidator parentSchema) {
     super(context, schemaNode, schemaParentNode, parentSchema);
 
@@ -91,7 +147,7 @@
         validated = !valueNode.isTextual() || DATE_PATTERN.matcher(valueNode.textValue()).matches();
         break;
       case FORMAT_DATE_TIME:
-        validated = !valueNode.isTextual() || DATETIME_PATTERN.matcher(valueNode.textValue()).matches();
+        validated = !valueNode.isTextual() || buildDateTimePattern().matcher(valueNode.textValue()).matches();
         break;
       case FORMAT_EMAIL:
         validated = !valueNode.isTextual() || EMAIL_PATTERN.matcher(valueNode.textValue()).matches();
--- openapi-schema-validator/src/test/resources/schema/format.json	2025-01-03 11:38:21.422387798 +0100
+++ openapi-schema-validator/src/test/resources/schema/format.json	2025-01-02 16:36:15.016420399 +0100
@@ -509,7 +509,7 @@
       {
         "description": "case-insensitive T and Z",
         "data": "1963-06-19t08:30:06.283185z",
-        "valid": true
+        "valid": false
       },
       {
         "description": "only RFC3339 not all of ISO 8601 are valid",
--- openapi-schema-validator/src/test/resources/schema/optional/format.json	2025-01-03 11:35:16.216168809 +0100
+++ openapi-schema-validator/src/test/resources/schema/optional/format.json	2025-01-02 16:36:24.004532281 +0100
@@ -31,7 +31,7 @@
             {
                 "description": "case-insensitive T and Z",
                 "data": "1963-06-19t08:30:06.283185z",
-                "valid": true
+                "valid": false
             },
             {
                 "description": "only RFC3339 not all of ISO 8601 are valid",
